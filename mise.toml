[tools]
opentofu = "1.11.4"
terraform-docs = "latest"

[settings]
experimental = true

# Lint and Format

[tasks.format]
run = "tofu fmt -recursive"

# Release

[tasks.start-release]
run = "./scripts/git-tag-release.sh"

# Documentation

[tasks.docs]
run = "terraform-docs ."
